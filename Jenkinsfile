pipeline {
		agent {
			label {
				label 'built-in'
				customWorkspace '/data/projects/vel-app'
			}
		}
		
		stages {
			stage ('stage-1') {
				steps {
					echo "this is a master branch"
				}
			}
		
		
		}
		
}
